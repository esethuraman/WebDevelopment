<div class="container">

<h2>New User</h2>

<%= render "form.html", Map.put(assigns, :action, user_path(@conn, :create)) %>

<span><%= link "Login Page", to: page_path(@conn, :index) %></span>

</div>